Load AI-optimized workflow context for beads issue tracking.

Outputs essential beads workflow rules and command reference to help agents remember to use fbd instead of markdown TODOs after context compaction.

```bash
fbd prime
```
